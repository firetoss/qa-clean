# 中文问答净化流水线依赖（pip回退安装，推荐使用conda）
# Python 3.11+ 支持
# 注意：FAISS必须通过conda-forge安装，pip无法安装

# 核心依赖
pandas>=2.1.0
openpyxl>=3.1.2
sentence-transformers>=2.2.2
torch>=2.1.0
psycopg2-binary>=2.9.9
scikit-learn>=1.3.2
numpy>=1.24.0
tqdm>=4.66.0
pyyaml>=6.0
regex>=2023.10.3

# 可选依赖
# matplotlib>=3.7.0  # 图表生成
# rapidfuzz>=3.5.0   # 高性能编辑距离

# 重要说明：
# FAISS GPU/CPU 需要通过 conda-forge 安装：
# conda install -c conda-forge faiss-gpu>=1.12.0
# 或者
# conda install -c conda-forge faiss-cpu>=1.12.0
